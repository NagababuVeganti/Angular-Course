import { InputForamtDirective } from './input-foramt.directive';

describe('InputForamtDirective', () => {
  it('should create an instance', () => {
    const directive = new InputForamtDirective();
    expect(directive).toBeTruthy();
  });
});
